{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "page": {
      "type": "string",
      "description": "Component page",
      "x-prompt": "What should the component be insert to?"
    },
    "name": {
      "type": "string",
      "description": "Component name",
      "x-prompt": "What should the component be called?"
    }
  },
  "required": ["page","name"]
}
